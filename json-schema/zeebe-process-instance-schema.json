{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/zeebe/refs/heads/main/json-schema/zeebe-process-instance-schema.json",
  "title": "Zeebe Process Instance",
  "description": "A running instance of a deployed BPMN process definition in the Zeebe workflow engine.",
  "type": "object",
  "properties": {
    "processInstanceKey": {
      "type": "integer",
      "format": "int64",
      "description": "Unique key identifying this process instance"
    },
    "processDefinitionKey": {
      "type": "integer",
      "format": "int64",
      "description": "Key of the deployed process definition this instance runs"
    },
    "bpmnProcessId": {
      "type": "string",
      "description": "BPMN process ID as defined in the process definition XML"
    },
    "version": {
      "type": "integer",
      "description": "Version of the process definition"
    },
    "tenantId": {
      "type": "string",
      "description": "Tenant identifier for multi-tenant deployments"
    }
  },
  "required": ["processInstanceKey", "processDefinitionKey", "bpmnProcessId"]
}
